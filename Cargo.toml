[package]
name = "tagged-urn"
version = "0.1.0"
edition = "2021"
description = "Tagged URN - Flat tag-based identifier system"
authors = ["FGND Team"]

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
regex = "1.0"

[dev-dependencies]
